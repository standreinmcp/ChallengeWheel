import {Animated, Easing} from 'react-native';
import {data} from '../constants';
import {metrics, colors} from '../themes/';

export const rotateAnimation = async ({toValue, spinValue, result}) => {
  
};
